/**
 * Uncategorized junk drawer of basic constants, types, utilities.
 */

// constants

// types

export enum Orientation {
    UP = 'up',
    DOWN = 'dn',
    LEFT = 'lf',
    RIGHT = 'rt',
    CUSTOM = '',

    DEFAULT = UP
}
